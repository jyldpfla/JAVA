do while 
: do 중괄호 안으로 들어가서 먼저 실행하고 while문의 조건을 확인해서 true면 다시 do문으로 돌아가서 반복

● switchcase (문자열 값 or 정수) {}
:()에 맞는 값을 찾아서 아래로 쭉 실행하다가 찾으면 출력하고 중괄호 밖 순으로 정상 출력, ()에 해당하는 case를 찾을 수 없을 때 default값 실행
- default값은 필수x, else문처럼 사용가능
- 찾아서 출력한 후 끝내고 싶으면 break
- case내 여러문장, if문등 대부분 사용 가능

